#!/bin/bash

# Get the PID of the current script
script_pid=$$

# Find processes whose name contains the word 'bash' and extract their PID and name
while read -r pid name; do
    echo "$pid $name"
done < <(pgrep -af bash | grep -v "$script_pid")
